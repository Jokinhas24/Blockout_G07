namespace BlockoutProject_G07
{
    /// <summary>
    /// Difficulty of the game, it is tied to an int which says the size of the board (int x int)
    /// </summary>
    public enum Difficulty
    {
        Easy = 3,
        Medium = 5,
        Hard = 8
    }
}